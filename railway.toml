[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build -w @lexpro/server"

[deploy]
startCommand = "npm start -w @lexpro/server"
healthcheckPath = "/api/version"
healthcheckTimeout = 30

[deploy.envVariables]
PORT = "3001"
