[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build -w @lexpro/server"

[deploy]
startCommand = "npm start -w @lexpro/server"
healthcheckPath = "/api/version"
healthcheckTimeout = 60

[deploy.envVariables]
PORT = "3001"
MONGODB_URI = "mongodb+srv://amr:amr@cluster0.nh9dfgl.mongodb.net/lexpro?retryWrites=true&w=majority&appName=Cluster0"
JWT_SECRET = "lexpro-jwt-secret-2024"
CLIENT_URL = "*"
